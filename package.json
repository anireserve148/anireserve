{
  "name": "anireserve-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "client": "cd apps/mobile-client && npm start",
    "pro": "cd apps/mobile-pro && npm start",
    "web": "cd web && npm run dev"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}